import ClaimForm from "./ClaimForm";

export default function Body() {
    return(
        <div className="flex">
            This is the body with the <ClaimForm/> element inside
        </div>
    );
}